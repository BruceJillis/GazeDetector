Enderman Gaze Detector
======================

A small but non-trivial minecraft mod that replaces vanilla mob-heads and adds an enderman head that emits redstone when viewed from a (configurable) distance. 